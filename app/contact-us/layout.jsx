export const metadata = {
  title: 'Contact Us | TechStack',
  description: 'TechStack by UCoder',
}  

export default function ContactUsLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}
